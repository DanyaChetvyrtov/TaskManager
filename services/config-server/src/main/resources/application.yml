spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DanyaChetvyrtov/TaskManager.git
          search-paths: configurations/{application}

server:
  port: 8071
